606:  m.route === `/${cleanUpper.toLowerCase()}` ||
757:onOpenOmnibar={() => setIsOmnibarOpen(true)}
758:onOpenProfile={() => setIsLoginModalOpen(true)}
759:onOpenDecisionAssistant={() => setIsDecisionAssistantOpen(true)}
760:onOpenGuidance={() => setIsGuidanceModalOpen(true)}
761:onOpenAcademy={() => setIsAcademyOpen(true)}
762:onOpenGlossary={() => setIsGlossaryOpen(true)}
763:onOpenWorkQueue={() => setIsWorkQueueOpen(true)}
764:onOpenNotifications={() => setIsNotificationsOpen(true)}
769:  onOpenUnifiedPipeline={() => setIsUnifiedPipelineOpen(true)}
770:  onOpenExport={() => setIsTableExportOpen(true)}
787:  onOpenOmnibar={() => setIsOmnibarOpen(true)}
788:onOpenWorkQueue={() => setIsWorkQueueOpen(true)}
789:onOpenProfile={() => setIsLoginModalOpen(true)}
790:onOpenNotifications={() => setIsNotificationsOpen(true)}
795:onOpenRecentFavorites={() => setIsRecentFavoritesOpen(true)}
796:onOpenPreferences={() => setIsSystemPreferencesOpen(true)}
798:  )}
812:    isFavorite={favorites.some(f => f.moduleId === currentModule.moduleId)}
813:    onToggleFavorite={() => handleToggleFavorite(currentModule)}
814:  onOpenGuidance={() => setIsGuidanceModalOpen(true)}
850:        onRefresh={() => addToast('info', 'Làm mới dữ liệu', 'Đã cập nhật dữ liệu từ Authoritative Core.')}
852:  onExport={() => setIsTableExportOpen(true)}
861:onOpenWorkQueue={() => setIsWorkQueueOpen(true)}
862:onOpenOmnibar={() => setIsOmnibarOpen(true)}
863:onOpenDecisionAssistant={() => setIsDecisionAssistantOpen(true)}
864:onOpenAcademy={() => setIsAcademyOpen(true)}
865:onOpenGlossary={() => setIsGlossaryOpen(true)}
872:        )}
881:        )}
888:        )}
895:        )}
902:        )}
908:        )}
920:        )}
932:        )}
957:        )}
974:        )}
982:        )}
990:        )}
997:        )}
1004:        )}
1011:        )}
1019:        )}
1028:        )}
1036:        )}
1043:        )}
1050:        )}
1057:        )}
1064:        )}
1072:        )}
1078:        )}
1085:        )}
1092:        )}
1099:        )}
1106:        )}
1113:        )}
1120:        )}
1127:        )}
1134:        )}
1141:        )}
1149:        )}
1157:onClose={() => setIsOmnibarOpen(false)}
1165:    onClose={() => setIsLoginModalOpen(false)}
1210:    onClose={() => setIsContextRailOpen(false)}
1224:    onClose={() => setIsRecentFavoritesOpen(false)}
1232:onNavigate={(mod) => setCurrentModule(mod)}
1239:onClose={() => setIsSystemPreferencesOpen(false)}
1248:onClose={() => setIsQuickPreviewOpen(false)}
1256:onClose={() => setIsPdfModalOpen(false)}
1265:onClose={() => setIsTableExportOpen(false)}
1275:onClose={() => setIsUnifiedPipelineOpen(false)}
1281:onClose={() => setIsDecisionAssistantOpen(false)}
1298:onClose={() => setIsGuidanceModalOpen(false)}
1314:    onClose={() => setIsAcademyOpen(false)}
1331:onClose={() => setIsGlossaryOpen(false)}
1347:  <ConfirmDialog dialog={confirmDialog} onClose={() => setConfirmDialog(null)} />
