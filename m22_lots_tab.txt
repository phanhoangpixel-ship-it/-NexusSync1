      {activeTab === 'lots' && (
        <div className="bg-white rounded-2xl border border-slate-200 shadow-xs overflow-hidden">
          <div className="p-4 border-b border-slate-200 flex flex-col sm:flex-row items-center justify-between gap-4">
            <div className="relative flex-1 sm:w-80">
              <Search className="w-4 h-4 text-slate-400 absolute left-3.5 top-3" />
              <input
                type="text"
                value={searchLotQuery}
                onChange={(e) => setSearchLotQuery(e.target.value)}
                placeholder="Tìm kiếm số lô, SKU, tên sản phẩm, kho..."
                className="w-full pl-10 pr-4 py-2 text-sm bg-slate-50 border border-slate-200 rounded-xl focus:outline-none focus:ring-2 focus:ring-indigo-500"
              />
            </div>
            <div className="flex items-center gap-3">
              <select 
                value={statusFilter}
                onChange={(e) => setStatusFilter(e.target.value)}
                className="px-3 py-2 text-sm bg-slate-50 border border-slate-200 rounded-xl focus:outline-none focus:ring-2 focus:ring-indigo-500 font-medium text-slate-700"
              >
                <option value="ALL">Tất cả trạng thái lô</option>
                <option value="ACTIVE">Hoạt động tốt (Active)</option>
                <option value="EXPIRED_SOON">Sắp hết hạn (Expiry Warning)</option>
                <option value="EXPIRED">Đã hết hạn / Cách ly</option>
              </select>
            </div>
          </div>

          <div className="overflow-x-auto">
            <table className="w-full text-left border-collapse">
              <thead>
                <tr className="bg-slate-50 text-slate-600 text-xs font-bold uppercase tracking-wider border-b border-slate-200">
                  <th className="p-4">Số Lô (Batch No) & SKU</th>
                  <th className="p-4">Tên sản phẩm & Kho</th>
                  <th className="p-4 text-center">Ngày sản xuất (Mfg)</th>
                  <th className="p-4 text-center">Hạn sử dụng (Exp)</th>
                  <th className="p-4 text-right">Tồn hiện tại</th>
                  <th className="p-4 text-center">Trạng thái Lô</th>
                  <th className="p-4 text-center">Thao tác & Drilldown</th>
                </tr>
              </thead>
              <tbody className="divide-y divide-slate-100 text-sm">
                {filteredLots.length === 0 ? (
                  <tr>
                    <td colSpan={7} className="p-8 text-center text-slate-500">
                      Không tìm thấy lô hàng nào phù hợp với bộ lọc tìm kiếm.
                    </td>
                  </tr>
                ) : (
                  filteredLots.map((lot) => (
                    <tr key={lot.id} className="hover:bg-slate-50/80 transition-colors group">
                      <td className="p-4">
                        <div className="font-semibold text-slate-900 font-mono">{lot.batchNumber}</div>
                        <div className="text-xs text-slate-500 font-mono">{lot.sku} • {lot.supplierLot}</div>
                      </td>
                      <td className="p-4">
                        <div className="font-medium text-slate-800">{lot.productName}</div>
                        <div className="text-xs text-slate-500">{lot.warehouse}</div>
                      </td>
                      <td className="p-4 text-center font-mono text-slate-600">{lot.mfgDate}</td>
                      <td className="p-4 text-center font-mono font-semibold text-slate-800">{lot.expDate}</td>
                      <td className="p-4 text-right font-mono font-bold text-emerald-700">
                        {lot.currentQty.toLocaleString()} <span className="text-xs font-normal text-slate-500">{lot.uom}</span>
                      </td>
                      <td className="p-4 text-center">
                        <span className={`px-3 py-1 text-xs font-semibold rounded-full ${
                          lot.status === 'ACTIVE' ? 'bg-emerald-100 text-emerald-800' :
                          lot.status === 'EXPIRED_SOON' ? 'bg-amber-100 text-amber-800' :
                          'bg-rose-100 text-rose-800'
                        }`}>
                          {lot.status === 'ACTIVE' ? 'Đạt chuẩn' :
                           lot.status === 'EXPIRED_SOON' ? 'Sắp hết hạn' : 'Đã hết hạn / Cách ly'}
                        </span>
                      </td>
                      <td className="p-4 text-center">
                        <div className="flex items-center justify-center gap-1.5">
                          <button
                            onClick={() => setDrilldownLot(lot)}
                            className="px-2.5 py-1.5 bg-indigo-50 hover:bg-indigo-100 text-indigo-700 text-xs font-semibold rounded-lg transition-colors flex items-center gap-1 shadow-2xs"
                            title="Mở drilldown lịch sử tồn kho & theo dõi hạn sử dụng trực quan"
                          >
                            <Clock className="w-3.5 h-3.5 text-indigo-600" />
                            <span>Drilldown</span>
                          </button>
                          <button
                            onClick={() => {
                              setSelectedId(lot.id);
                              setActiveTab('traceability');
                            }}
                            className="p-1.5 bg-slate-100 hover:bg-slate-200 text-slate-700 rounded-lg transition-colors"
                            title="Truy xuất nguồn gốc lô"
                          >
                            <Eye className="w-4 h-4" />
                          </button>
                          {lot.status !== 'EXPIRED' && (
                            <button
                              onClick={() => handleQuarantineLot(lot.id, lot.batchNumber)}
                              className="p-1.5 bg-rose-50 hover:bg-rose-100 text-rose-700 rounded-lg transition-colors"
                              title="Đưa vào cách ly"
                            >
                              <Trash2 className="w-4 h-4" />
                            </button>
                          )}
                        </div>
                      </td>
                    </tr>
                  ))
                )}
              </tbody>
            </table>
          </div>
        </div>
      )}

      {/* Tab: FEFO Simulator */}
