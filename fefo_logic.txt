      {activeTab === 'fefo' && (
        <div className="space-y-6">
          <div className="bg-white p-6 rounded-2xl border border-slate-200 shadow-xs">
            <h2 className="text-lg font-bold text-slate-900">Mô phỏng Đề xuất Xuất Kho Theo Quy tắc FEFO (First Expired, First Out)</h2>
            <p className="text-sm text-slate-600 mt-1">Hệ thống WMS tự động sắp xếp các lô hàng theo thứ tự hạn sử dụng gần nhất để đề xuất thủ kho xuất hàng trước nhằm giảm thiểu hàng tồn kho quá hạn.</p>
          </div>

          <div className="bg-white rounded-2xl border border-slate-200 shadow-xs overflow-hidden">
            <div className="p-4 border-b border-slate-200 bg-indigo-50/50 flex items-center justify-between">
              <span className="font-bold text-indigo-900 text-sm">Thứ tự ưu tiên xuất kho cho SKU: SKU-ENG-088 (Động cơ servo AC)</span>
              <span className="text-xs font-semibold bg-indigo-100 text-indigo-800 px-3 py-1 rounded-full">Thuật toán FEFO Active</span>
            </div>
            <div className="p-6 space-y-4">
              {[...lots].sort((a, b) => a.expDate.localeCompare(b.expDate)).map((lot, idx) => (
                <div key={lot.id} className="flex flex-col md:flex-row items-start md:items-center justify-between p-4 rounded-xl border border-slate-200 bg-slate-50 gap-4">
                  <div className="flex items-center gap-4">
                    <div className={`w-10 h-10 rounded-xl flex items-center justify-center font-bold font-mono text-white ${
                      idx === 0 ? 'bg-rose-600 shadow-md' : idx === 1 ? 'bg-amber-600' : 'bg-indigo-600'
                    }`}>
                      #{idx + 1}
                    </div>
                    <div>
                      <div className="flex items-center gap-2">
                        <span className="font-bold text-slate-900 font-mono">{lot.batchNumber}</span>
                        <span className="text-xs text-slate-500">({lot.warehouse})</span>
                      </div>
                      <div className="text-xs text-slate-600 mt-0.5">Sản phẩm: <strong>{lot.productName}</strong></div>
                    </div>
                  </div>
                  <div className="flex items-center gap-4">
                    <div className="text-right">
                      <div className="text-xs text-slate-500">Hạn sử dụng</div>
                      <div className="text-sm font-bold text-rose-700 font-mono">{lot.expDate}</div>
                    </div>
                    <div className="text-right">
                      <div className="text-xs text-slate-500">Tồn khả dụng</div>
                      <div className="text-sm font-bold text-emerald-700 font-mono">{lot.currentQty} {lot.uom}</div>
                    </div>
                    <span className={`px-3 py-1 text-xs font-bold rounded-lg ${
                      idx === 0 ? 'bg-rose-100 text-rose-800 animate-pulse' : 'bg-slate-200 text-slate-700'
                    }`}>
                      {idx === 0 ? 'Ưu tiên xuất số 1 (FEFO)' : 'Đợi xuất sau'}
                    </span>
                    <button
                      onClick={() => setDrilldownLot(lot)}
                      className="px-2.5 py-1.5 bg-white hover:bg-slate-100 text-slate-700 border border-slate-300 rounded-lg text-xs font-semibold flex items-center gap-1.5 transition-colors shadow-2xs"
                      title="Xem drilldown lịch sử tồn kho & theo dõi hạn sử dụng"
                    >
                      <Clock className="w-3.5 h-3.5 text-indigo-600" />
                      <span>Chi tiết</span>
                    </button>
                  </div>
                </div>
              ))}
            </div>
          </div>
        </div>
      )}

      {/* Tab: Traceability */}
